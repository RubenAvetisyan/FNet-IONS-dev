import { getPayments, connect } from "#imports"

export default defineEventHandler(() => {
    // const app = await getPayments(connect())
    // console.log('app: ', app);
    const dbResponse = ''
    return 'Done'
})
